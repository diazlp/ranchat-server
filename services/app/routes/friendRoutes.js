const express = require("express");
const router = express.Router();

// router.post("/:userId", ) //add friend
// router.get("/:userId", ) //friend list online or offline
// router.patch("/:userId",) //accept friend request
// router.delete("/:userId",) //delete friend
module.exports = router;
